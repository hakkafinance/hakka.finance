const buttons = {
  primary: {
    color: 'red',
    bg: 'blue',
  },
};

const fontSizes = [];

const colors = {
  background: 'black',
  primary: 'tomato',
};

const space = [0, 4, 8, 12, 16, 32, 64];

const defaultTheme = {
  colors: {
    background: 'black',
    primary: 'tomato',
  },
};

export default defaultTheme;
